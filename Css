/* General styles */
body {
    background-color: black;
    font-family: 'Trebuchet MS', sans-serif;
    color: white;
    text-align: center;
    margin: 0;
    padding: 0;
}

/* Header styles */
h1 {
    font-size: 36px;
    margin-top: 20px;
}

h2 {
    font-size: 18px;
    margin: 20px;
    line-height: 1.6;
}

p {
    font-size: 24px;
    margin: 30px 0;
}

/* Link styles */
a {
    background-color: white;
    font-size: 20px;
    border-radius: 10px;
    padding: 10px;
    display: inline-block;
    text-align: center;
    margin: 10px auto;
    color: black;
    text-decoration: none;
    width: 200px;
    transition: opacity 1s ease-in-out, transform 1s ease-in-out;
    opacity: 0; 
    transform: translateY(20px); 
}

a:hover {
    background-color: black;
    color: white;
}

/* Animation for links when they appear */
.link-container.show a {
    opacity: 1;
    transform: translateY(0); 
}

/* Menu icon styles */
.menu-icon {
    position: absolute;
    top: 20px;
    right: 20px;
    cursor: pointer;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 4px;
}

.menu-icon .dot {
    width: 6px;
    height: 6px;
    background-color: white;
    border-radius: 50%;
}

/* Dropdown menu styles */
.dropdown-menu {
    position: absolute;
    top: 50px;
    right: 20px;
    background-color: black;
    color: white;
    border-radius: 8px;
    padding: 10px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    text-align: left;
    display: flex;
    flex-direction: column;
}

.dropdown-menu.hidden {
    display: none;
}

.dropdown-menu a {
    color: white;
    text-decoration: none;
    padding: 5px 0;
    font-size: 16px;
    transition: color 0.3s ease;
}

.dropdown-menu a:hover {
    color: #007BFF;
}
